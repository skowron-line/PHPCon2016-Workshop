{% extends '::base.html.twig' %}

{% block body %}
<div class="row marketing">
    <div class="col-lg-12">
        {% for workshop in workshops %}
            {% if workshop.display %}
                {{ render(controller('AppBundle:Workshop:workshop', {
                    'id': workshop.id
                })) }}
            {% endif %}
        {% endfor %}
    </div>

</div>
{% endblock %}